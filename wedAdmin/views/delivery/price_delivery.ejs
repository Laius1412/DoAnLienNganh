<!-- Content -->
<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="h3 mb-0">Quản lý Giá cước chuyển phát</h2>
        <div class="d-flex gap-3"> 
            <div>
                <button class="btn btn-primary" id="btnAddPrice">
                    <i class="bi bi-plus-circle me-1"></i>Thêm bảng giá
                </button>
            </div>
            <div>
                <button class="btn btn-danger" id="btnDeleteSelected" disabled>
                    <i class="bi bi-trash me-1"></i>Xóa đã chọn
                </button>
            </div>
        </div>
    </div>

    <!-- Price List Section -->
    <div class="card">
        <div class="card-body">
            <div id="priceDeliveryList"></div>
        </div>
    </div>
</div>

<!-- Modal Thêm/Sửa -->
<div class="modal fade" id="priceModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <form class="modal-content" id="priceForm">
            <div class="modal-header">
                <h5 class="modal-title" id="priceModalTitle">Thêm bảng giá</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <!-- Region Selection -->
                <div class="row mb-4">
                    <div class="col-md-6">
                        <label for="fromRegion" class="form-label">Khu vực 1</label>
                        <select class="form-select" id="fromRegion" name="fromRegionId" required>
                            <option value="">Chọn khu vực 1</option>
                        </select>
                    </div>
                    <div class="col-md-6">
                        <label for="toRegion" class="form-label">Khu vực 2</label>
                        <select class="form-select" id="toRegion" name="toRegionId" required>
                            <option value="">Chọn khu vực 2</option>
                        </select>
                    </div>
                </div>

                <!-- Weight Ranges Section -->
                <div class="card">
                    <div class="card-header bg-light">
                        <div class="d-flex justify-content-between align-items-center">
                            <h6 class="mb-0">Các mức giá theo khối lượng</h6>
                            <button type="button" class="btn btn-outline-success btn-sm" id="btnAddRange">
                                <i class="bi bi-plus-circle me-1"></i>Thêm mức giá
                            </button>
                        </div>
                    </div>
                    <div class="card-body">
                        <div id="weightRanges"></div>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="bi bi-x-circle me-1"></i>Hủy
                </button>
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-check-circle me-1"></i>Lưu
                </button>
            </div>
        </form>
    </div>
</div>

<!-- Modal Xác nhận xóa -->
<div class="modal fade" id="deleteConfirmModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Xác nhận xóa</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>Bạn có chắc chắn muốn xóa các bảng giá đã chọn?</p>
                <p class="text-danger mb-0"><small>Lưu ý: Hành động này không thể hoàn tác.</small></p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="bi bi-x-circle me-1"></i>Hủy
                </button>
                <button type="button" class="btn btn-danger" id="btnConfirmDelete">
                    <i class="bi bi-trash me-1"></i>Xóa
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Bootstrap Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
<!-- Custom JS -->
<script src="/js/price_delivery.js"></script>